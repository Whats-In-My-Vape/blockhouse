<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="org.wit.blockhouse">

  <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

  <application
      android:name=".main.MainApp"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme.Launcher"
      android:fullBackupContent="@xml/backup_descriptor">

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyCy3B4ni4-AYRg7H6hB3Q-zo73Y3zyQgm4" />


    <activity
        android:name=".views.auth.LoginView"
        android:label="@string/title_activity_login"
        android:launchMode="singleTop">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>


    <activity
        android:name=".views.blockhouseList.BlockhouseListView"
        android:label="@string/title_activity_blockhouse_list"
        android:launchMode="singleTop"></activity>

    <activity
        android:name=".views.blockhouse.BlockhouseView"
        android:label="@string/title_activity_blockhouse">
      <meta-data
          android:name="android.support.PARENT_ACTIVITY"
          android:value=".views.blockhouseList.BlockhouseListView" />
    </activity>

    <activity
        android:name=".views.editLocation.EditLocationView"
        android:label="@string/title_activity_edit_location">
      <meta-data
          android:name="android.support.PARENT_ACTIVITY"
          android:value=".views.blockhouse.BlockhouseView" />
    </activity>

    <activity
        android:name=".views.map.BlockhouseMapView"
        android:label="@string/title_activity_blockhouse_maps"
        android:launchMode="singleTop">
      <meta-data
          android:name="android.support.PARENT_ACTIVITY"
          android:value=".views.blockhouseList.BlockhouseListView" />
    </activity>
    <!--<activity
      android:name=".activities.MapActivity"
        android:label="@string/title_activity_map" >
    </activity>

    <activity android:name=".activities.BlockhouseActivity"></activity>
    <activity android:name=".activities.BlockhouseListActivity"/>
    <activity android:name=".authorisation.ArrivalActivity"><intent-filter>
      <action android:name="android.intent.action.MAIN" />
      <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter></activity>
    <activity android:name=".authorisation.LoginActivity" />
    <activity android:name=".authorisation.RegisterActivity" />
    <activity android:name=".authorisation.UserSettings"/>
    <activity android:name=".activities.StatisticsActivity"/>-->
  </application>

</manifest>